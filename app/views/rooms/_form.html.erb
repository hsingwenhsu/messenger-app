<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3" >
    <h3><%= form.label "Add a new room"%></h3>
    <%= form.text_field :name, class: 'form-control', placeholder: "Room name", id: 'room_name_id'  %>
  </div>

  <%= form.submit "Submit", class: "btn btn-primary btn-block", id: 'new_room_submit' %>
<% end %>
