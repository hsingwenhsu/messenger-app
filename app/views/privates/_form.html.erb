<%= form_with(model: private, local: true) do |form| %>
  <% if private.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(private.errors.count, "error") %> prohibited this private from being saved:</h2>

      <ul>
        <% private.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
